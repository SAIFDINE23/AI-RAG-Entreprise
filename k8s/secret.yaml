# ============================================================
# Enterprise RAG Assistant — Kubernetes Secret
# ============================================================
# ⚠️  En production, utilisez un secret manager (Vault, Sealed Secrets, etc.)
#     Ne commitez JAMAIS ce fichier avec de vraies clés.
#     Les valeurs ci-dessous sont des placeholders à remplacer.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: rag-assistant-secrets
  namespace: rag-assistant
  labels:
    app.kubernetes.io/name: rag-assistant
    app.kubernetes.io/part-of: enterprise-rag-assistant
type: Opaque
stringData:
  GOOGLE_API_KEY: "REPLACE_ME"
  GROQ_API_KEY: "REPLACE_ME"
